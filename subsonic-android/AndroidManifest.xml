<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:a="http://schemas.android.com/apk/res/android"
          package="net.sourceforge.subsonic.android"
          a:versionCode="1"
          a:versionName="1.0">

    <uses-permission a:name="android.permission.INTERNET"/>

    <application a:label="Subsonic" a:icon="@drawable/launch">

        <activity a:name="MainActivity"
                  a:label="Subsonic">
            <intent-filter>
                <action a:name="android.intent.action.MAIN"/>
                <category a:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity a:name="SelectArtistActivity"
                  a:label="Browse music"/>

        <activity a:name="SelectAlbumActivity"
                  a:label="Browse music"/>

        <activity a:name="SettingsActivity"
                  a:label="Settings"/>

        <service a:name="DownloadService"
                 a:label="Download"/>

    </application>

</manifest>
