<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:a="http://schemas.android.com/apk/res/android"
              a:orientation="vertical"
              a:gravity="center_vertical"
              a:layout_width="fill_parent"
              a:layout_height="fill_parent">

    <Button
            a:id="@+id/main_browse"
            a:layout_width="fill_parent"
            a:layout_height="wrap_content"
            a:drawableLeft="@android:drawable/ic_menu_view"
            a:drawablePadding="20dp"
            a:gravity="left|center_vertical"
            a:textAppearance="?android:attr/textAppearanceLarge"
            a:text="Browse music"/>

    <Button
            a:id="@+id/main_settings"
            a:layout_width="fill_parent"
            a:layout_height="wrap_content"
            a:drawableLeft="@android:drawable/ic_menu_preferences"
            a:drawablePadding="20dp"
            a:gravity="left|center_vertical"
            a:textAppearance="?android:attr/textAppearanceLarge"
            a:text="Settings"/>

    <Button
            a:id="@+id/main_queue"
            a:layout_width="fill_parent"
            a:layout_height="wrap_content"
            a:drawableLeft="@android:drawable/ic_menu_gallery"
            a:drawablePadding="20dp"
            a:gravity="left|center_vertical"
            a:textAppearance="?android:attr/textAppearanceLarge"
            a:text="Download queue"/>

</LinearLayout>

